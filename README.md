# fs_optimization_rec_tool
A simulated File System Recovery and Optimization Tool that manages files, directories, and storage blocks using inodes and bitmaps. It supports reading, writing, deletion, journaling-based crash recovery, and provides an interactive GUI for efficient file system operations
